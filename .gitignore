# dependencies
node_modules
.pnp
.pnp.js

# next
.next
out

# env — every variant, so a stray .env.production cannot be committed
.env*
!.env.example

# debug / os
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# typescript
next-env.d.ts
*.tsbuildinfo

# raw yt-dlp dumps (regenerate with npm run ingest:fetch)
data/raw
